<span class="menu-label"></span>
<div class="menu-list">
<% @catalog.siblings.each do |catalog| %>
  <%= link_to catalog.name, { action: 'show', slug: catalog.path }, class: active_helper(slug: catalog.path, active: 'is-active') %>
<% end %>
</div>
