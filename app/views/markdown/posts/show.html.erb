<div data-id="<%= @post.id %>" class="content p-4">
  <h1><%= @post.title %></h1>
  <%= render partial: 'time', locals: { post: @post } %>
  <%= @post.html.html_safe %>
</div>

<%= render partial: 'md_layout/work_layout', collection: @post.block_texts, as: :item %>

<% content_for :title do %><%= @post.title %><% end %>
