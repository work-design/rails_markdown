<% Markdown::Catalog.default_where(default_params).nav.each do |catalog| %>
  <%= link_to catalog.name, { controller: '/markdown/posts', action: 'index', catalog_path: catalog.path }, class: active_params(catalog_path: catalog.path, active: 'navbar-item is-active', item: 'navbar-item') %>
<% end %>
